#pragma once

#include<stdio.h>
#include <iostream>
#include <string>
#include <vector>

using namespace std;
typedef short PROC;

class PKB {
public:
	static bool getModifies (/* Stmt stmtlineno, Var var */);
    static
};